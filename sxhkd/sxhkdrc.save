# https://github.com/bbaserdem/dotfiles/blob/master/sxhkd/sxhkdrc
# ~~~~~~~~~~~~~~~
# GENERAL HOTKEYS
# ~~~~~~~~~~~~~~~
# Terminal Emulator
super + Return
	$TERMINAL

# Reload config files
super + shift + Escape
	pkill -USR1 -x sxhkd


# ~~~~~~~~~~~~~
# BSPWM HOTKEYS
# ~~~~~~~~~~~~~
# Quit BSPWM normally
super + alt + Escape
	bspc quit

# Close and kill
super + {_,shift + }q
	bspc node --{close,kill}

# Alternate between tiled and monocle layout
# super + alt + 1
# 	bspc desktop --layer next

# Swap the current node and biggest node
super + g
	bspc node --swap biggest

#
# State/Flags
#
# Set the window state
super + shift + {t,s,f,m}
	bspc node --state {tiled,pseudo_tiled,floating,fullscreen}

# Set the node flags 
super + ctrl + {x,y,z}
	bspc node --flag {locked,sticky,private}


